package com.gestionStock.GestionStock.mapper;

import com.gestionStock.GestionStock.dto.CommandeClientDto;
import com.gestionStock.GestionStock.model.CommandeClient;
import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface CommandeClientMapper {

//    CommandeClientDto fromEntity(CommandeClient commandeClient);
//
//    CommandeClient toEntity(CommandeClientDto commandeClientDto);
}
